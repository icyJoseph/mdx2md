import { Button } from './Button';
import styles from './styles.css';
export const meta = { title: 'Esm Only' };
export default function Page() { return null; }

# ESM only fixture

Paragraph with no MDX syntax.
